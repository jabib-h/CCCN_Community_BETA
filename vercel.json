{
  "builds": [
    {
      "src": "api/main.py",
      "use": "@vercel/python",
      "config": { "includeFiles": ["api/**", "web/**"] }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/main.py" }
  ]
}
